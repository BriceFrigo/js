let saisie  = prompt("Veuillez saisir un nombre !");
do{
    let saisie  = prompt("Veuillez saisir un nombre !");
    if(saisie <= 100 && saisie >= 50){
        alert("gg")
    }
}
while (saisie >= 100 || saisie <= 50);